# Production Environment Variables
# Copy this file to your web hosting and rename it to .env
# IMPORTANT: Make sure your MongoDB Atlas has whitelisted your server's IP address

NODE_ENV=production
PORT=5000

# MongoDB Atlas Connection (REQUIRED for production)
# This stores sessions in MongoDB so they persist across server restarts
MONGODB_URI=mongodb+srv://terning:ultraVGA1280%21%3F@cluster0.ybtknfs.mongodb.net/ticket_management?retryWrites=true&w=majority&appName=Cluster0

# Session Secret (CHANGE THIS to a strong random string in production!)
SESSION_SECRET=your-super-secret-session-key-change-this-to-random-string

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here

# IMPORTANT PRODUCTION CHECKLIST:
# 1. Whitelist your server's IP in MongoDB Atlas Network Access
# 2. Make sure your web server is serving over HTTPS (required for secure cookies)
# 3. Run: npm install connect-mongo (required for MongoDB session storage)
# 4. Restart your Node.js server after updating this file
